
<% content_for(:html_title) { 'Cost Center - Index' } %>

<!-- START card -->
<div class="card card-transparent">
    <div class="card-header">
     <div class="pull-left">
       <%= link_to 'New Cost Center', new_costcenter_path, :class => 'btn btn-primary btn-cons' %>
     </div>
     <div class="pull-right">
       <div class="col-xs-12">
         <input type="text" id="search-table" class="form-control pull-right" placeholder="Search">
       </div>
     </div>
     <div class="clearfix"></div>
   </div>
   <div class="card-body">
     
     <table class="table table-hover demo-table-search table-responsive-block" id="tableWithSearch">
       <thead>
         <tr>
          <th>Code</th>
          <th>Name</th>
           <th>Action</th>
         </tr>
       </thead>
       <tbody>
        <% @costcenters.each do |costcenter| %>
         <tr>
         
          <td class="v-align-middle semi-bold"><%= costcenter.code %></td>
          <td class="v-align-middle"><%= costcenter.name %></td>
        
          <td class="v-align-middle"><%= link_to 'Show', costcenter %> |
          <%= link_to 'Edit', edit_costcenter_path(costcenter) %> |
          <%= link_to 'Delete', costcenter, method: :delete, data: { confirm: 'Are you sure to Delete Cost Center ?'  } %></td>
          
         
         
         </tr>
          <% end %>
       </tbody>
     </table>
    
   </div>
</div>






