
<% content_for(:html_title) { 'Department - Edit' } %>

<div class="card card-default">

	<%= render 'form', department: @department %>

	<br>
	<%= link_to 'Show', @department, :class => 'btn btn-complete btn-cons' %> 
	<%= link_to 'Back', departments_path, :class => 'btn btn-complete btn-cons' %>

</div>
<br>